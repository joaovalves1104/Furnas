O que está preinstalado localmente:
!pip install pandas matplotlib sklearn numpy json statsmodels

Quando executado deve-se instalar os seguintes pacotes:
!pip install --quiet fastapi kaleido python-multipart uvicorn networkx==2.8.8 pgmpy

Deve-se alterar o arquivo "DynamicBayesianNetwork.py" após a instalação de pgmpy